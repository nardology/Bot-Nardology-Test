# =============================================================================
# Bot-Nardology Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# Lines starting with # are comments. Remove the # to set a value.
#
# REQUIRED vars are marked [REQUIRED].
# Others have sensible defaults or are optional.
# =============================================================================


# ---------------------------------------------------------------------------
# Discord  [REQUIRED]
# ---------------------------------------------------------------------------

# Your Discord bot token (from https://discord.com/developers/applications)
DISCORD_TOKEN=

# Bot display name (cosmetic, used in logs and messages)
# BOT_NAME=Bot-Nardology


# ---------------------------------------------------------------------------
# Environment
# ---------------------------------------------------------------------------

# "prod" or "dev". Controls command syncing, DB fallback, and validation strictness.
# ENVIRONMENT=prod


# ---------------------------------------------------------------------------
# Database  [REQUIRED in production]
# ---------------------------------------------------------------------------

# Postgres connection URL. In dev mode, falls back to local SQLite.
# Format: postgres://user:password@host:port/dbname
DATABASE_URL=

# Set to "1" to auto-create tables on startup (useful for first deploy).
# DB_AUTO_CREATE=false


# ---------------------------------------------------------------------------
# Redis
# ---------------------------------------------------------------------------

# Redis connection URL. Used for caching, rate limits, pack storage, etc.
# The bot degrades gracefully if Redis is unavailable.
# REDIS_URL=redis://localhost:6379


# ---------------------------------------------------------------------------
# OpenAI / AI
# ---------------------------------------------------------------------------

# OpenAI API key for /talk AI conversations.
# OPENAI_API_KEY=sk-...

# AI model names (defaults shown). gpt-4.1-mini for Pro, gpt-4.1-nano for Free.
# OPENAI_MODEL=gpt-4.1-mini
# OPENAI_MODEL_FREE=gpt-4.1-nano

# Base URL for the OpenAI-compatible API.
# OPENAI_BASE_URL=https://api.openai.com/v1

# Request timeout in seconds.
# OPENAI_TIMEOUT_S=20

# Emergency kill switch: set to "true" to disable ALL AI calls immediately.
# AI_DISABLED=false

# Daily AI cost cap per guild in cents. Prevents runaway spend.
# AI_COST_CAP_FREE_DAILY_CENTS=5
# AI_COST_CAP_PRO_DAILY_CENTS=50


# ---------------------------------------------------------------------------
# Stripe (Payments & Subscriptions)
# ---------------------------------------------------------------------------

# Stripe secret key (from https://dashboard.stripe.com/apikeys)
# Required for premium subscriptions, gifts, and point purchases.
# STRIPE_SECRET_KEY=sk_live_...

# Stripe webhook signing secret (from your webhook endpoint settings).
# REQUIRED in production -- webhooks are rejected without it.
# STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe Price ID for the Pro monthly subscription.
# Create in Stripe Dashboard -> Products -> Prices.
# STRIPE_PRICE_PRO_MONTHLY=price_...

# Stripe Price IDs for points bundles (100 points per $1 USD).
# STRIPE_PRICE_POINTS_500=price_...
# STRIPE_PRICE_POINTS_1000=price_...
# STRIPE_PRICE_POINTS_2500=price_...
# STRIPE_PRICE_POINTS_5000=price_...
# STRIPE_PRICE_POINTS_10000=price_...

# Gift premium price in cents (default: $4.99 = 499 cents per month).
# STRIPE_GIFT_UNIT_AMOUNT_CENTS=499

# Redirect URLs after Stripe Checkout completes or is cancelled.
# STRIPE_SUCCESS_URL=https://discord.com
# STRIPE_CANCEL_URL=https://discord.com


# ---------------------------------------------------------------------------
# Bot Owners
# ---------------------------------------------------------------------------

# Comma-separated Discord user IDs with owner privileges.
# These users can access /z_owner, /z_server, and packadmin commands.
# BOT_OWNER_IDS=123456789012345678,987654321098765432


# ---------------------------------------------------------------------------
# Guild Sync (Development)
# ---------------------------------------------------------------------------

# In dev mode, slash commands are synced to these guild(s) for instant updates.
# In prod mode, commands sync globally (takes up to 1 hour).
# DEV_GUILD_ID=123456789012345678
# SYNC_GUILD_ID=123456789012345678


# ---------------------------------------------------------------------------
# Custom Pack Limits
# ---------------------------------------------------------------------------

# Max custom packs per guild (non-owners). Pro guilds get unlimited.
# MAX_CUSTOM_PACKS_PER_GUILD=3

# Max characters per pack.
# MAX_CUSTOM_CHARS_PER_PACK=25

# Max total custom characters per guild.
# MAX_CUSTOM_CHARS_TOTAL_PER_GUILD=100
# MAX_CUSTOM_CHARS_TOTAL_PER_GUILD_PRO=250


# ---------------------------------------------------------------------------
# One-Time Maintenance Flags
# ---------------------------------------------------------------------------

# Set to "true" for one startup, then set back to "false".
# CLEANUP_DEV_COMMANDS_ONCE=false
# CLEAR_GLOBAL_COMMANDS_ONCE=false
